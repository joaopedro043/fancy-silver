function tocaSom(seletorAudio){      
    document.querySelectorAll(seletorAudio).play('')
     const elemento = listaDeTecla = document.querySelectorAll('.tecla');

}
if(elemento === null){
   elemento.play();
    while(contator < 9){
    const instrumento = listadeteclas[contador];
        listaDeTecla[contator].onclick = function(){
    
    };
    contator = contator +1;}
    tecla.onclick = function(){
        tocaSom(idAudio);
    };
     tecla.onkeydown = function(){
if(evento.code === 'Space' || evento.code === 'enter'){
       
   
  
    }
tecla.onkeyup = function(){
tecla.classListe.remove('ativa');
}